# 校园二手交易平台

> Campus Second-Hand Trading Platform（CSHTP）

---

## 一、项目定位

一款面向高校学生的 **C2C 校园二手交易平台**，覆盖**商品发布 → 检索浏览 → 下单购买 → 库存扣减 → 通知触达**的完整交易链路。项目以 **MyBatis 深度使用** 为骨架，内置 **乐观锁防超卖** 与 **多线程异步化** 两条并发主线，用于巩固 MyBatis 全栈能力并**初步建立高并发编程意识**（JDK 原生多线程入门，不越级）。

> **技术栈（严格限定）**：Java + Spring Boot + MyBatis + MySQL  
> **明确不做**：不引入 Redis、MQ、Spring Cloud、分布式锁、`@Async`、`CompletableFuture` 等超纲内容，所有并发能力均基于 JDK 原生 `Thread` / `ExecutorService` + 数据库乐观锁实现。

---

## 二、项目规模

| 维度 | 数量 |
|------|------|
| 数据库表 | 7 张 |
| RESTful 接口 | 约 22 个 |
| 核心模块 | 4 个（用户与认证、商品与分类、订单与库存、通知与操作日志） |
| 代码包层级 | controller / service / mapper / entity / dto / vo / common / config / exception / enums / aspect / util |
| 并发主线 | 2 条（乐观锁防超卖、多线程异步化） |

---

## 三、项目目录结构树

```
src/main/java/com/cshtp/
├── CshtpApplication.java               # Spring Boot 启动类（@MapperScan）
├── common/
│   ├── Result.java                     # 统一响应体 Result<T>
│   ├── PageResult.java                 # 分页响应体
│   └── BusinessException.java          # 通用业务异常基类
├── config/
│   ├── MybatisConfig.java              # MyBatis：mapper-locations、type-aliases、驼峰映射
│   ├── OpenApiConfig.java              # Knife4j / OpenAPI 3 配置
│   ├── WebMvcConfig.java               # 全局日期格式、跨域、统一返回包装
│   └── ThreadPoolConfig.java           # JDK 原生线程池 ExecutorService 配置
├── controller/
│   ├── UserController.java             # 用户注册/登录/信息接口
│   ├── CategoryController.java         # 分类管理接口
│   ├── ProductController.java          # 商品发布/搜索/详情接口
│   ├── OrderController.java            # 下单/支付/订单查询接口（核心并发）
│   └── NotificationController.java     # 通知列表接口
├── service/
│   ├── UserService.java
│   ├── CategoryService.java
│   ├── ProductService.java
│   ├── OrderService.java               # 核心：下单事务编排 + 乐观锁 + 异步通知
│   ├── NotificationService.java
│   └── impl/
│       ├── UserServiceImpl.java
│       ├── CategoryServiceImpl.java
│       ├── ProductServiceImpl.java
│       ├── OrderServiceImpl.java
│       └── NotificationServiceImpl.java
├── mapper/
│   ├── UserMapper.java
│   ├── CategoryMapper.java
│   ├── ProductMapper.java              # 含乐观锁 deductStock
│   ├── OrderMapper.java
│   ├── OrderItemMapper.java
│   ├── NotificationMapper.java
│   └── OperationLogMapper.java
├── entity/
│   ├── User.java
│   ├── Category.java
│   ├── Product.java                    # 含 version 乐观锁字段
│   ├── Order.java                      # 订单主表，共享 orderNo
│   ├── OrderItem.java                  # 订单项，一对多
│   ├── Notification.java
│   └── OperationLog.java
├── dto/
│   ├── UserRegisterDTO.java            # @NotBlank、@Pattern 手机号
│   ├── ProductCreateDTO.java
│   ├── OrderCreateDTO.java            # 含 productId + quantity 列表
│   └── PageQueryDTO.java               # 分页+模糊查询入参
├── vo/
│   ├── ProductVO.java                  # 含分类名、卖家昵称（多表组装）
│   ├── OrderVO.java                    # 含订单项列表
│   └── OrderItemVO.java
├── exception/
│   ├── BusinessException.java          # 已迁至 common，保留扩展
│   ├── StockNotEnoughException.java    # 库存不足 / 乐观锁冲突
│   └── GlobalExceptionHandler.java     # @RestControllerAdvice 全局处理
├── enums/
│   ├── OrderStatus.java                # PENDING / PAID / CANCELLED
│   └── ProductStatus.java              # ON_SALE / OFF_SHELF
├── aspect/
│   ├── LogOperationAspect.java         # @AfterReturning / @AfterThrowing 操作日志
│   └── LogOperation.java               # 自定义注解
└── util/
    ├── SnowflakeIdGenerator.java       # 雪花算法订单号生成器
    └── ThreadPoolHolder.java           # 线程池工具类（submit 异步任务）

src/main/resources/
├── application.yml                     # 数据源、MyBatis、日志配置
└── mapper/                             # Mapper XML 文件（按模块拆分）
    ├── UserMapper.xml
    ├── CategoryMapper.xml
    ├── ProductMapper.xml                # <update id="deductStock"> 乐观锁
    ├── OrderMapper.xml
    ├── OrderItemMapper.xml
    ├── NotificationMapper.xml
    └── OperationLogMapper.xml

sql/
└── init.sql                            # 建表 + 初始化数据

src/test/java/com/cshtp/
├── mapper/                             # Mapper 单元测试（@MybatisTest）
├── service/                            # Service 层测试（含并发扣库存）
└── controller/                         # MockMvc 接口测试
```

---

## 四、数据库表设计（MySQL）

### 4.1 表清单

| 表名 | 主键 | 外键 / 唯一约束 | 核心字段 | 说明 |
|------|------|------------------|----------|------|
| `user` | `id` bigint | `username` 唯一 | `username`, `password`, `phone`, `nickname` | 用户表 |
| `category` | `id` bigint | `name` 唯一 | `name`, `description` | 商品分类（一级） |
| `product` | `id` bigint | `seller_id` 索引 | `title`, `price` decimal(10,2), `stock` int, `status` tinyint, `category_id`, `seller_id`, **`version` int** | 商品表（version 乐观锁） |
| `order` | `id` bigint | `order_no` 唯一 | `order_no` varchar(64), `buyer_id`, `total_price` decimal(10,2), `status` tinyint | 订单主表 |
| `order_item` | `id` bigint | `order_id` → order.id | `order_id`, `product_id`, `quantity`, `price` | 订单项（一对多） |
| `notification` | `id` bigint | 无 | `user_id`, `content`, `is_read`, `create_time` | 异步通知表 |
| `operation_log` | `id` bigint | 无 | `user_id`, `action`, `method`, `detail`, `create_time` | 操作日志表 |

### 4.2 表间关系

```
category 1 ──< product >── 1 user (seller)
                                  │
order 1 ──< order_item >── 1 product
   │
   └── notification (多对一 user，下单后异步生成)
```

- **order ↔ order_item**：一对多，一个订单含多个商品项
- **order_item → product**：多对一，每项指向一个商品（扣库存依据）
- **product → category / seller(user)**：多对一
- **notification → user**：多对一，下单后异步写入买家通知

### 4.3 核心约束

- **乐观锁防超卖**：`product.version` 在 UPDATE 时校验，保证库存不会被并发覆盖
- **订单号唯一**：`order_no` 由雪花算法生成，全局唯一且有序
- **库存非负**：扣减 SQL 附加 `stock >= #{quantity}` 兜底
- **下单原子性**：扣库存 + 生成订单 + 生成订单项在同一 `@Transactional` 内

---

## 五、核心功能模块

### 5.1 用户与认证模块

| 接口 | 方法 | 路径 | 技术点 |
|------|------|------|--------|
| 注册 | POST | `/api/users/register` | `@Valid` + DTO 校验（`@NotBlank`, `@Pattern` 手机号） |
| 登录 | POST | `/api/users/login` | Session / Token（任选其一，建议 Session 简化） |
| 查询个人信息 | GET | `/api/users/{id}` | 参数校验、VO 脱敏（不返密码） |
| 修改个人信息 | PUT | `/api/users/{id}` | `@Valid` + 更新校验 |

**知识点映射**：`@Validated`、`@NotBlank`、`@Pattern`、DTO/VO 分层、密码加密（可选 BCrypt）

### 5.2 商品与分类模块

| 接口 | 方法 | 路径 | 技术点 |
|------|------|------|--------|
| 发布商品 | POST | `/api/products` | `@Valid` + DTO，关联 `category_id`、`seller_id` |
| 编辑商品 | PUT | `/api/products/{id}` | 校验归属（只能编辑自己的商品） |
| 分页搜索商品 | GET | `/api/products` | PageHelper + `<where>` + `<if>` 动态条件（分类/关键词/价格区间） |
| 商品详情 | GET | `/api/products/{id}` | `resultMap` 多表关联（association：分类、卖家） |
| 分类管理 | POST/GET | `/api/categories` | CRUD + 分页 |

**知识点映射**：MyBatis 动态 SQL、`<foreach>` 批量、PageHelper 分页、`resultMap` 一对一/一对多关联

### 5.3 订单与库存模块（核心并发模块）

| 接口 | 方法 | 路径 | 技术点 |
|------|------|------|--------|
| 下单 | POST | `/api/orders` | `@Transactional` 事务、乐观锁扣库存、生成订单+订单项、异步通知 |
| 支付（模拟） | PUT | `/api/orders/{orderNo}/pay` | 修改订单状态，校验幂等 |
| 查询订单详情 | GET | `/api/orders/{orderNo}` | `resultMap` 关联订单项列表 |
| 我的订单（买家/卖家） | GET | `/api/orders` | PageHelper 分页 + 条件筛选 |

**下单事务编排**（伪代码）：

```java
@Transactional(rollbackFor = Exception.class)
public OrderVO createOrder(OrderCreateDTO dto) {
    String orderNo = snowflake.nextId();
    // 1. 锁定并扣减每个商品库存（乐观锁，返回影响行数）
    for (Item item : dto.getItems()) {
        int rows = productMapper.deductStock(item.getProductId(), item.getQuantity(), item.getVersion());
        if (rows == 0) throw new StockNotEnoughException(...);   // 库存不足或版本冲突 → 回滚
    }
    // 2. 生成订单主记录
    Order order = orderMapper.insert(new Order(orderNo, buyerId, total));
    // 3. 生成订单项
    orderItemMapper.batchInsert(items);
    // 4. 事务提交后异步：发通知 + 记日志（见 6.2）
    return toVO(order);
}
```

**知识点映射**：`@Transactional`、`resultMap` 一对多、乐观锁 version、事务边界与异步解耦

### 5.4 通知与操作日志模块（多线程入门模块）

| 接口 | 方法 | 路径 | 技术点 |
|------|------|------|--------|
| 查询我的通知列表 | GET | `/api/notifications` | PageHelper 分页、按 `is_read` 筛选 |
| 标记已读 | PUT | `/api/notifications/{id}/read` | 简单更新 |

**异步写入**（下单成功后，通过线程池提交）：

```java
// OrderServiceImpl 中，事务提交后触发
ThreadPoolHolder.submit(() -> {
    notificationMapper.insert(new Notification(buyerId, "下单成功：" + orderNo));
    operationLogMapper.insert(new OperationLog(userId, "下单", "Order", orderNo, "SUCCESS"));
});
```

**知识点映射**：JDK `ExecutorService` 线程池、`Runnable`、事务外异步、线程池销毁 `shutdown()`

---

## 六、技术实现亮点

### 6.1 MyBatis 动态 SQL + 多表关联 + 分页

**动态条件搜索**（商品分页搜索）：

```xml
<select id="findPage" resultMap="ProductVOMap">
    SELECT p.*, c.name AS category_name, u.nickname AS seller_name
    FROM product p
    LEFT JOIN category c ON p.category_id = c.id
    LEFT JOIN user u ON p.seller_id = u.id
    <where>
        <if test="keyword != null">
            AND p.title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="categoryId != null">
            AND p.category_id = #{categoryId}
        </if>
        <if test="minPrice != null">
            AND p.price &gt;= #{minPrice}
        </if>
    </where>
    ORDER BY p.create_time DESC
</select>
```

**多表关联 resultMap**（商品详情 VO）：

```xml
<resultMap id="ProductVOMap" type="ProductVO">
    <id     column="id"            property="id"/>
    <result column="title"          property="title"/>
    <result column="price"          property="price"/>
    <association property="category" javaType="Category">
        <result column="category_name" property="name"/>
    </association>
    <association property="seller"   javaType="User">
        <result column="seller_name"  property="nickname"/>
    </association>
</resultMap>
```

**批量插入**（订单项）：

```xml
<insert id="batchInsert">
    INSERT INTO order_item (order_id, product_id, quantity, price) VALUES
    <foreach collection="list" item="item" separator=",">
        (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.price})
    </foreach>
</insert>
```

**知识点映射**：`<if>`/`<where>`、`<foreach>`、`association` 一对一、`PageHelper` 分页

### 6.2 乐观锁防超卖 + 多线程异步化（并发主线）

**乐观锁扣库存 SQL**：

```xml
<update id="deductStock">
    UPDATE product
    SET stock   = stock - #{quantity},
        version = version + 1
    WHERE id      = #{productId}
      AND version = #{version}
      AND stock  &gt;= #{quantity}
</update>
```

- **校验逻辑**：若返回影响行数 = 0，说明 `version` 已变（被其他线程改过）或 `stock < quantity`，抛 `StockNotEnoughException` 触发事务回滚
- **无死锁**：按 `productId` 升序扣减，减少锁竞争
- **重试策略**（可选进阶）：捕获冲突后最多重试 3 次（手动循环 + `Thread.sleep(10)`）

**下单与异步解耦**：

```
┌────────────┐    ┌──────────────────┐    ┌──────────────┐    ┌────────────────────┐
│ ① 创建订单 │ ──→│ ② 乐观锁扣库存   │ ──→│ ③ @Transactional │ ──→ │ ④ 事务提交后异步   │
│ 生成orderNo│    │ version 校验     │    │ 全或无提交    │    │ 通知 + 操作日志    │
└────────────┘    └──────────────────┘    └──────────────┘    └────────────────────┘
                                                       │                    │
                                                       ↓                    ↓
                                              库存准确、订单完整     线程池执行，主线程不阻塞
```

- **核心下单逻辑**（扣库存 + 订单 + 订单项）置于 `@Transactional` 内，保证原子性
- **异步任务**（通知、日志）通过 `ThreadPoolHolder.submit()` 提交，**不在同一事务中**，避免异步失败拖累主流程

### 6.3 全局异常处理体系

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(StockNotEnoughException.class)
    public ResponseEntity<Result<Void>> handleStock(StockNotEnoughException e) {
        return badRequest(Result.error("库存不足或已被其他用户抢购，请刷新重试"));
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<Result<Void>> handleValid(MethodArgumentNotValidException e) {
        // 提取 @Valid 校验失败字段信息
        String msg = e.getBindingResult().getAllErrors().get(0).getDefaultMessage();
        return badRequest(Result.error(msg));
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<Result<Void>> handleOther(Exception e) {
        return internalError(Result.error("系统异常：" + e.getMessage()));
    }
}
```

### 6.4 AOP 操作日志

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface LogOperation {
    String action();      // "下单" / "发布商品" 等
    String targetType();  // "Order" / "Product" 等
}
```

```java
@Aspect
@Component
public class LogOperationAspect {
    @Autowired OperationLogMapper logMapper;

    @AfterReturning(pointcut = "@annotation(logOp)", returning = "result")
    public void logSuccess(JoinPoint jp, LogOperation logOp, Object result) {
        logMapper.insert(new OperationLog(... , "SUCCESS"));
    }

    @AfterThrowing(pointcut = "@annotation(logOp)", throwing = "ex")
    public void logFailure(JoinPoint jp, LogOperation logOp, Exception ex) {
        logMapper.insert(new OperationLog(... , "FAILED: " + ex.getMessage()));
    }
}
```

> **注意**：异步写日志时通过 `ThreadPoolHolder.submit()` 提交，避免阻塞主流程；线程池在 Bean 销毁时 `shutdown()`。

### 6.5 接口文档自动化（Knife4j / springdoc-openapi）

```java
@Configuration
public class OpenApiConfig {
    @Bean
    public OpenAPI cshtpOpenAPI() {
        return new OpenAPI()
            .info(new Info().title("CSHTP API").version("1.0").description("校园二手交易平台"))
            .components(new Components().addSecuritySchemes(/* 预留 */));
    }
}
```

每个 Controller / 方法 / DTO 字段均标注：
- 类级别：`@Tag(name = "订单", description = "下单、支付、查询")`
- 方法级别：`@Operation(summary = "下单", description = "扣库存 + 生成订单，含乐观锁防超卖")`
- 字段级别：`@Schema(description = "购买数量，必须大于0", example = "2")`

### 6.6 统一响应与参数校验

```java
public class Result<T> {
    private int code;
    private String message;
    private T data;
    // success / error / warning 静态工厂方法
}

public class PageResult<T> {
    private long total;
    private int pageNum;
    private int pageSize;
    private List<T> list;
}
```

DTO 校验示例：

```java
public class UserRegisterDTO {
    @NotBlank(message = "用户名不能为空")
    private String username;

    @Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")
    private String phone;
}
```

### 6.7 DTO / VO 严格分层

| 类型 | 用途 | 典型注解 |
|------|------|----------|
| DTO（入参） | 接收前端请求体/参数 | `@NotBlank`, `@Pattern`, `@Valid` 嵌套校验 |
| Entity | MyBatis 持久化映射 | 与表字段一一对应，`version` 乐观锁 |
| VO（出参） | 返回前端展示 | 多表组装、扁平化、脱敏（不含密码） |

---

## 七、个人职责与收获

### 职责

- 独立完成需求分析、数据库设计与技术选型，从零搭建 Spring Boot + MyBatis 项目骨架
- 负责全部后端代码开发（4 个模块、约 22 个接口），编写 Knife4j 注解与单元测试
- 设计下单核心事务编排逻辑与乐观锁防超卖机制，实现多线程异步通知与操作日志

### 收获

- **MyBatis 深度运用**：动态 SQL、多表关联（resultMap）、PageHelper 分页、批量操作全面覆盖，摆脱对 JPA 的简单依赖
- **并发意识启蒙**：通过乐观锁 version 真正理解「丢失更新」问题，掌握 JDK 原生线程池在异步化场景的使用，而非停留在"加个注解"的层面
- **事务边界理解**：明确区分「核心下单逻辑（事务内）」与「通知日志（事务外异步）」，理解事务一致性与性能的平衡

---

## 八、难点与解决方案

### 难点一：下单的并发超卖控制

**问题**：多笔下单同时修改同一商品库存时，可能出现丢失更新（两个线程都读到旧库存，各自计算后覆盖），导致库存扣成负数。

**方案**：
- `product` 表加 `version` 字段，扣减 SQL 附加 `WHERE version = ? AND stock >= ?`
- Service 层判断 UPDATE 返回行数，为 0 则抛 `StockNotEnoughException` 触发回滚
- 事务内按 `productId` 升序扣减，减少死锁概率
- （进阶）捕获冲突后最多重试 3 次（手动循环 + `Thread.sleep(10)`）

### 难点二：下单主流程与通知日志的性能解耦

**问题**：下单成功后若同步写通知表 + 操作日志表，会增加响应耗时；但若完全异步又需保证"订单已提交成功"的前提。

**方案**：
- 核心下单逻辑（扣库存 + 订单 + 订单项）置于 `@Transactional(rollbackFor = Exception.class)` 内保证原子性
- 通过 `ThreadPoolHolder`（包装 `Executors.newFixedThreadPool(2)`）在事务提交后异步执行通知与日志写入
- 线程池在 Bean 销毁时调用 `shutdown()` 优雅关闭，避免线程泄漏

### 难点三：异步任务不在同一事务中的一致性权衡

**问题**：异步写通知/日志若与下单同事务，异步失败会导致订单回滚；若完全无关，可能出现"订单成功但通知丢失"。

**方案**：
- 采用**最终一致性**思路：下单事务成功提交是前提，异步任务基于已提交的事实执行
- 通知/日志属辅助数据，允许极端情况下重试，不影响核心交易
- 失败日志通过 AOP 的 `@AfterThrowing` 记录，供审计排查

### 难点四：MyBatis 多表关联的结果映射

**问题**：商品详情需同时返回分类名、卖家昵称，若手动多次查询组装代码冗余。

**方案**：
- 使用 `resultMap` 的 `<association>` 一对一映射，单条 SQL JOIN 完成组装
- 订单详情使用 `<collection>` 一对多映射订单项列表
- 列表查询统一走 VO + JOIN，避免 N+1 查询

---

## 九、学习路线与里程碑

### 9.1 阶段划分（不跳步）

| 阶段 | 内容 | 产出 |
|------|------|------|
| 阶段 0 | 环境搭建：Spring Boot + MyBatis + MySQL，跑通首个 Mapper | 可运行的空工程 |
| 阶段 1 | MyBatis 补全：动态 SQL → 多表关联 → PageHelper 分页 | 商品搜索/详情可用 |
| 阶段 2 | 业务开发：统一响应、全局异常、参数校验、Knife4j、四大模块接口 | 完整 CRUD + 文档 |
| 阶段 3 | 并发进阶：乐观锁防超卖 + 多线程异步通知/日志 | 无超卖 + 异步写入 |
| 阶段 4 | 测试完善：Mapper 单测、MockMvc 接口测试、README、Git tag | 可交付项目 |

### 9.2 并发学习路径（循序渐进）

| 步骤 | 内容 | 对应模块 | 难度 |
|------|------|----------|------|
| 1 | `Thread` / `Runnable` 基础 | 异步通知 | ⭐ |
| 2 | 线程池 `ExecutorService` | 通知 + 日志 | ⭐⭐ |
| 3 | 乐观锁 `version` 解决并发冲突 | 下单扣库存 | ⭐⭐ |
| 4 | 并发测试（JMeter 压测验证无超卖） | 库存准确性 | ⭐⭐ |

> **明确不做**（留给后续进阶）：`@Async`、`CompletableFuture`、Redis 缓存、分布式锁、消息队列。当前目标仅为**建立多线程与并发控制的直观认识**。

### 9.3 里程碑排期

| 里程碑 | 阶段 | 预计耗时 | 产出 |
|--------|------|----------|------|
| M0 | 阶段 0 | 0.5 天 | 可运行空工程 + 首个 Mapper |
| M1 | 阶段 1 | 2~3 天 | 动态 SQL、多表、分页可用 |
| M2 | 阶段 2 | 3~4 天 | 四大模块接口 + Knife4j 文档 |
| M3 | 阶段 3 | 2~3 天 | 乐观锁防超卖 + 异步化 |
| M4 | 阶段 4 | 1~2 天 | 单元测试 + README |

**总计约 8~12 天**（每天 2~3 小时），可按自身节奏调整。

---

## 十、风险与边界说明

1. **技术栈严格限定**：全程仅 Java + Spring Boot + MyBatis + MySQL，不引入 Redis、MQ、Spring Cloud 等中间件
2. **不跳步**：MyBatis 从动态 SQL → 多表 → 分页 → 并发，逐层递进
3. **并发难度可控**：仅 JDK 原生多线程 + 乐观锁，先建立概念，后续再学 `@Async`、分布式锁
4. **简历包装边界**：技术亮点均为真实可实现内容，建议如实描述为「个人实战项目，练习并发控制与异步处理」

---

*— 文档结束 —*
